class Menu extends Phaser.Scene {
    constructor () {
        super("menuScene");
    }

    create() {
        // add any text

        // would like clickable buttons to move to play credits instruction scenes

        // set up key input
    }

    update() {
        // check if the buttons are clicked and move scene
    }
}